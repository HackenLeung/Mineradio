# Changelog

## v1.3.3

### 播放与歌词稳定性

- 修复本地歌曲偶发起播后进度停在 `0:00`、明明已经缓冲却要等待数秒的问题：输出设备和 WebAudio 音频图现在会在 `play()` 前完成准备，起播后不再重复切换；本地文件若仍遇到 Chromium 媒体时钟冻结，会在约 1.6 秒内进入现有重建恢复流程，在线歌曲和中途恢复的等待策略不变。
- 修复暂停时歌词偶尔跳到另一首歌或显示错误“暂无歌词”标题的问题：网络、缓存、本地在线匹配、延迟歌词兜底和翻译补偿在写入前都会确认仍属于当前队列歌曲；晚到的旧请求被安全丢弃，同一歌曲的本地元数据回填仍可正常更新歌词。

### 桌面与启动

- 修复从音乐魔方或托盘主动隐藏主窗口后，页面启动过程中的 `dom-ready`、`did-finish-load` 或兜底定时器又把窗口显示出来的问题；明确点击托盘、魔方打开主程序和第二实例唤起仍会正常显示窗口。
- 修复页面已经可用、但个别子资源加载慢时被 15 秒 `loadURL` 超时误判为“主窗口加载失败”的问题；真正未到可用状态或渲染进程异常的启动失败仍会保留错误提示。

### 启动性能

- 启动模块由浏览器同步逐个请求 111 个文件改为服务端并行读取、一次返回；仍保持同步注入，以保证 splash 和音频监视器现有的 `DOMContentLoaded` 初始化顺序。合并端点异常时自动回退为原逐个加载流程。

### 性能优化

- 修复 high 档 1440p@1.5x 和 4K 分辨率下歌词纹理压缩失效导致显存翻倍的 bug：压缩触发条件从 `sourceWidth <= widthBudget + 8` 改为 `sourceWidth < widthBudget * 0.95`，影响 1440p@1.5x 从 270MB/60行 降到 ~130MB。
- 新增 GPU 显存探测：通过 `WEBGL_debug_renderer_info` 识别 NVIDIA/AMD/Intel 型号并估算显存，回退方案为 `MAX_TEXTURE_SIZE` 或 `deviceMemory` 推断。
- 新增歌词质量自适应降级：加载长歌词前检查显存压力，超 15-25% 自动 high→balanced→eco 降档或限制常驻行数，防止 4GB 内存 512MB 显存机器 OOM 崩溃。
- Google Fonts 改为异步加载（`media="print" onload="this.media='all'"`），不再阻塞首屏渲染，墙内/断网环境首屏快 3-10 秒。

## v1.3.2

- 新增舞台式 3D 音乐库封面墙：支持本地歌曲、文件夹和在线歌单的分层浏览，并在本地歌曲页提供搜索、定位当前歌曲、悬浮“下一首播放”和回到顶部操作。
- 新增音乐库歌曲封面投递：点击卡片后封面以 70% 起步沿抛物线送入播放器，配合节奏联动的 Three.js 音波粒子、控制条玻璃动画和歌词舞台缩放；过期回调不会再覆盖或重播旧歌曲，损坏封面会立即回退普通播放。
- 新增本地音乐封面管理：保留文件夹、完整本地元数据和在线匹配结果；封面按自定义、同目录、内嵌和在线结果排序复用。
- 新增月度与年度听歌报告：基于有效播放时长生成本地统计和海报，支持查看、清除与导出，不为数据不足的排名补造内容。
- 修复本地歌曲智能过渡：本地播放分支会主动准备下一首；sidecar/内嵌歌词可用于计算人声进入点，已匹配在线元数据时按对应身份查缓存和接口，未匹配歌曲不再请求 `id=undefined`。
- 修复快照与“恢复上次队列”只恢复播放模式却不刷新按钮的问题；非法持久化模式会归一为顺序循环。
- 持续加固播放稳定性：完善旧请求、媒体时钟冻结、长暂停恢复、音源回退和智能过渡的所有权与进度校验。

- 新增局域网遥控：手机浏览器扫码或输入 6 位配对码即可控制播放、音量、静音和桌面歌词，无需安装任何应用。开关在视觉控制台「歌词」栏的桌面歌词分组内。
- 局域网遥控使用独立监听器，主服务端口始终只绑回环。关闭开关即销毁监听器，端口不再存在；切换开关不会重启主服务，正在播放的音频流不中断。遥控端口在关闭后重开会拿回同一个端口，已配对手机不需要重新扫码。
- 遥控鉴权：配对码 6 位、10 分钟有效，配对码与二维码常驻显示，随时可加设备；配对成功、过期或被作废后自动换新码，界面上的二维码始终可用。轮询刷新不会换码。连续输错 5 次立即作废并保留提示，可手动换码。长期 token 为 32 字节随机值，定长比较，落盘保存以便重启后免重扫，上限 16 台按最久未活跃淘汰。视觉控制台可一键移除所有已配对设备。
- 修复局域网遥控面板在配对完成后出现空白占位框的问题：`index.css` 没有全局 `[hidden]` 兜底规则，作者样式的 `display` 会盖掉 hidden 属性，导致元素被清空内容后仍然占位。
- 修复视觉控制台和遥控地址列表的复制按钮无效：`clipboard-write` 不在本地权限白名单内，`navigator.clipboard` 会被权限处理器静默拒绝。复制统一走主进程原生剪贴板，并保留 `navigator.clipboard` 与 `execCommand` 两级兜底。
- 遥控接口按来源分层：局域网来源只能访问遥控页面和 `/api/remote/*` 中的配对、状态、命令三个端点；本地音乐流、听歌数据、平台账号等其余接口以及配对码、状态上报、撤销、监听器起停等管理接口只服务本机。遥控监听器上的请求一律按外部来源处理，本机回环连接遥控端口同样拿不到管理接口。
- 修复浏览器网页可借本机回环地址跨域读取配对码并开启遥控监听器的问题：遥控与诊断响应不再开放通配 CORS，本机管理接口校验同源 Origin，所有有副作用的遥控请求只接受 JSON，跨站简单请求无法完成配对或下发命令。
- 修复遥控端口在较低候选端口占用情况变化后发生漂移的问题：实际端口会持久化为首选值，关闭或重启后优先复用；快速连续开关按请求顺序串行执行，旧响应不会恢复已经关闭的状态。
- 遥控地址改用内核选路结果确定（UDP 连接取出网源地址），不再依赖网卡名匹配；修复 Hyper-V 虚拟交换机 `vEthernet (Default Switch)` 被当作可用地址、二维码指向不通地址的问题。地址列表标注系统出网网卡与虚拟网卡，点击任意地址可切换二维码。
- 本地服务端默认只监听 `127.0.0.1`，不再默认绑定所有网卡；新增 `MINERADIO_HOST` 环境变量用于显式放开。
- 新增媒体时钟冻结检测：播放中途时钟连续 1 秒不推进且通过归属校验后，向本机 `/api/diag/stall-log` 记录一条诊断，含播放位置、readyState、networkState、缓冲末端、音频上下文状态和距上次 `waiting` 的间隔。同一次冻结只记一条，浏览器访问该端点可查看，日志文件跨重启保留。此项只观测不干预，不改变现有恢复行为。
- 修复智能过渡触发瞬间音量突然放大又回落的问题：增益拆分为元素与节点两级时，两级各持 `sqrt(v)`，而读取函数返回的是单级值，导致过渡开始时按 `sqrt` 域数值重新开方，实际输出跳到 `sqrt(v)`。音量越低越明显（30% 时约 +5 dB，20% 时约 +7 dB）。
- 修复诊断日志与已配对设备的存放位置随启动方式漂移的问题：原先跟随的缓存根在 Electron 下解析到 `node_modules/electron/dist`（`npm install` 会删除重建），裸跑 Node 时解析到项目目录，同一台机器出现两份数据。现由主进程显式注入，统一放在缓存根的 `diagnostics` 和 `remote` 子目录。
- 视觉控制台新增控件登记校验：控制台按布局清单搬运控件后会删除未登记的节点，此前漏登记会导致控件被静默删除且无任何报错。
- 修复本地歌曲几乎不触发智能过渡的问题：`playQueueAt` 对本地歌走独立分支并提前 return，其末尾的过渡排程走不到，`playLocalQueueSong` 自身又没有排程，`smartTransitionPending` 始终为空。此前只有手动拖动进度条或改动过渡样式/提前量才会补上排程，所以表现为时灵时不灵。
- 修复本地歌曲淡入总是从 0:00 开始的问题：淡入点靠下一首的第一句人声时间反推，而本地歌的歌词存在 `localLyricText`/`embeddedLyrics` 上、不在过渡准备阶段读取的 `song.lyric` 上，且没有在线 id 会让歌词端点退化成 `/api/lyric?id=undefined`。现在准备阶段直接读 sidecar 与内嵌歌词，已匹配在线元数据的按该身份查缓存和接口，未匹配的不再发这次必然落空的请求。手动重新匹配仍优先于文件自带歌词，与播放时的取词顺序一致。
- 修复重启后播放模式图标与实际行为不一致的问题：快照恢复只给 `playMode` 赋值，而按钮的唯一一次初始化在模块加载时就已执行完（当时还是默认的顺序循环），恢复后没有再刷新。表现为退出前用随机播放、重启后图标显示顺序循环但切歌仍是随机。首页「恢复上次队列」有同样的遗漏，一并修复；存档里的非法模式值现在会归一到顺序循环，不再让按钮停在切换列表找不到的状态上。

## v1.3.1

- 修复切歌时旧歌曲异步播放请求干扰新歌曲的问题，并加固播放失败后的当前媒体与 token 校验。
- 音频代理将上游开放式 Range 请求限制为 1 MiB 分段，同时保留显式和后缀 Range。
- 修复桌面歌词窗口生命周期清理时误清除启用状态的问题。
- 更新入口改为启动即显示、点击后手动检查；统一标题栏按钮尺寸、SVG 图标和线条样式。

## v1.3.0

- 同步原项目后续模块化前端架构和 Home、账号、歌单、长列表等能力，并把维护版已有的本地音乐、智能过渡、桌面歌词、Wallpaper Engine、桌面锁定、音乐魔方遥控器、均衡器和视觉控制台完整融合到新架构。
- 新增/完善小云、小Q、小狗多平台账号与歌单流程。
- 本地音乐详情增加“定位到当前歌曲”；修复已添加文件夹、完整本地元数据、内嵌封面/歌词和在线匹配信息的恢复与复用。
- 本地歌词自动匹配限定小云、小狗、小Q，优先当前登录平台；已有可用歌词和匹配缓存直接复用，持久缓存保留 `tlyric`、`ytlrc`、逐字歌词、罗马音和来源字段。
- 批量歌词匹配为小云、小狗、小Q设置共享请求间隔；识别 `405`、`操作频繁`、`稍候再试` 和 rate limit 响应后执行 4-30 秒指数冷却，排队任务会重新检查最新冷却时间。
- 保留智能过渡的音频、封面和视觉联动，完整删除独立的专辑“无缝衔接”引擎和 Cuefield AutoMix 运行时，避免多套过渡引擎互相抢占音频。
- 音源回退改为让正常播放路径解析候选，播放失败时恢复原队列项；加固长暂停恢复、小Q会员/授权状态、播放所有权和无可用音源终态。
- 正式版 `userData` 和 `sessionData` 固定使用 `<安装目录>\user-data`；Chromium 缓存使用同级 `<安装目录>\MineradioCache`，不迁移或逐文件合并到 AppData。
- 覆盖安装优先采用注册表记录的原安装目录，允许目录中保留 `user-data` 和 `MineradioCache`；旧数据缺失或不可写时明确停止，避免创建空白 profile。
- 安装器只覆盖程序文件；默认卸载保留 `user-data` 与 `MineradioCache`，只有用户明确勾选删除用户数据时才清理。
- 完整播放队列、当前歌曲、本地库、桌面歌词、托盘、下载设置、视觉/歌词/均衡器设置继续从同一个完整 Electron profile 读取。

## v1.2.6

- 继续听恢复完整播放队列（不再截到 300 首）和当前歌曲，仅在写入超过浏览器存储配额时才逐级收窄保存范围。
- 手动排入“下一首播放”的歌曲及其先后顺序随会话保存，重开后按原顺序还原。
- 过渡歌单改用网格填充率作为能量指标（`cameraBeats` 数 / 完整网格拍数），不再用与 BPM 共线的每秒拍数；同一速度下抒情曲和电子曲的能量档位能明显区分。能量分带版本号随缓存持久化，公式升级后旧分数自动重算。
- 过渡歌单取消 ±4 BPM 硬门槛，改为把速度差折算成能量当量参与排序，并允许半速 / 倍速衔接；结果更容易凑满目标数量，不再因 BPM 估计误差漏掉合适的歌。
- 过渡歌单新增“本地+已听在线”范围，可把听过且已有节奏缓存的在线歌纳入候选。
- 过渡歌单新增“扫描本地库节奏”，可批量建立节奏缓存，进度可见且随时可停。
- 过渡歌单“替换队列并播放”文案更明确，列表滚动条改为贴合系统的细样式。

## v1.2.5

- Wallpaper Engine Scene 改为通过原生命令打开 `project.json`，不再把低清 `preview.jpg` / `preview.gif` 拉伸为 Mineradio 全屏背景；支持扫描 Steam 库并显示媒体类型与预览分辨率。
- 新增 Wallpaper Engine 联动开关和背景暗度；切换失败时保留当前背景，关闭联动后立即恢复 Mineradio 星河、图片或视频背景。
- 新增“锁定到桌面”，将 Mineradio 嵌入 Windows WorkerW 桌面层；锁定时使用全屏展示布局并隐藏播放器及右侧操作入口，托盘可退出锁定。
- WorkerW 锁定使用真实显示器坐标，分辨率、缩放或显示器变化后自动重新铺满，并阻止锁定期间再次进入 Electron 原生全屏。
- 本地音乐搜索优先显示歌名命中结果；启动星河转场保持高帧，启动预览待机保持 30 FPS。
- 移除壁纸镜像。

## v1.2.4

- 本地音乐优先读取内嵌封面和内嵌歌词（FLAC 等），封面按“自定义 > 同目录 > 内嵌 > 在线”排序；内嵌歌词作为本地歌词源，不依赖在线接口即可显示。
- 内嵌封面每次启动即时重读、不写入缓存；内嵌歌词写入持久缓存，旧缓存在补读到歌词后自动回写。
- 修复本地歌曲在线匹配时主源命中 Live/翻唱即提前返回、不再查找真正的录音室版导致匹配失败的问题：有完整内嵌身份时，只有主源结果足够确信（高分、专辑或时长一致、非衍生版）才直接用主源，否则继续查第二源合并后再判定。
- 修复在线匹配封面覆盖本地内嵌封面的问题。

## v1.2.3

- 桌面歌词重启后记住开关状态和上次放置位置；手动关闭后仍保持关闭，不会强制常开。
- 桌面歌词相关选项移到视觉控制台「歌词」栏，歌词溢光等开关保留可用。

## v1.2.2

- 随机播放下“下一首播放”始终优先执行指定歌曲，并修复队列重排后连续返回上一首可能错歌的问题。
- 左侧歌单面板改为仅歌曲列表区域滚动，标题、标签页、模式和搜索工具栏保持固定。
- 本地歌曲队列操作收纳为悬浮工具栏，保留常显关闭按钮；下载入口替换为“打开所在文件夹”。
- 本地音乐默认继续使用文件名，播放时按需解析音频内嵌歌曲名、歌手和专辑；文件夹支持主动批量匹配歌词及实时进度。
- 音频标签、在线匹配和歌词结果加入持久缓存；批量解析结束后统一异步写盘，并增加失败重试、容量上限和过期清理。
- 修复音频标签解析成功但在线匹配失败时播放页和歌曲列表不能立即更新的问题。

## v1.2.1

- 智能过渡播放链路加固：切歌后正确恢复时间、音量和封面状态，失败时自动回退普通播放。
- 桌面歌词透明窗口范围收紧为紧凑尺寸，保留锁定穿透、中键解锁和长歌词滚动。
- 修复 Windows 125% 等高 DPI 缩放下拖动魔方遥控器时窗口尺寸反复放大、出现透明遮罩的问题；异常历史尺寸会自动纠正。
- 悬浮遥控器开启后继续保留系统托盘；鼠标移到遥控器上可用滚轮按 5% 调节音量；本地歌曲异步匹配到封面后会立即同步到魔方、条形和星稀月白遥控器。
- 更新缓存统一跟随安装目录的 `user-data\updates`；打包时移除默认 `app-update.yml`，并在启动后自动清理旧 `%LOCALAPPDATA%\mineradio-updater` 缓存。

## v1.2.0

- 其他应用进入前台全屏时，悬浮音乐遥控器会自动隐藏；退出全屏后自动恢复且不抢焦点。
- 全屏检测排除 Windows 桌面与任务栏窗口，避免点击桌面时误隐藏遥控器。
- 全屏监听器异常退出后按退避间隔自动重启，关闭遥控器或退出软件时会完整清理。
- 移除标题栏更新入口的红色提示点，保留更新检查与下载进度圆环。

## v1.1.9

- 视觉控制台「高级」右侧新增「控制」页，接入悬浮音乐遥控器（魔方 / 条形 / 星稀月白）。
- 遥控器开关与皮肤以桌面行为配置为唯一真源，拖动位置保存改为防抖写入。
- 开启遥控器后隐藏系统托盘，可通过遥控器打开/隐藏主程序，封面走本机代理加载。
- 本地库搜索后播放：播放队列与搜索结果一致，点播放后清空搜索。
- 随机播放只作用于下一首；上一首优先回到刚听过的歌曲，没有历史时再按队列回退。
- 标题栏下载中心、引导、更新入口按钮统一为 30×30。

## v1.1.8

- 打包后用户数据默认放到安装目录旁的 `user-data`，支持 `MINERADIO_USER_DATA` 覆盖。
- 安装器栈与路径校验加固，允许仅含 `user-data` 的目录重装接管。
- 卸载时可选择删除设置、缓存和登录状态；补全安装标记与更新残留文件清理。

## v1.1.7

- 修复智能过渡进入第二首时封面粒子短暂消失的问题。
- 保持预加载封面和深度纹理，并清除交接阶段残留的加载动画状态。

## v1.1.5

### 平台展示

- 界面、提示和项目文档中的三平台展示名统一为小云、小Q、小狗。
- 内部 `netease`、`qq`、`kugou` 协议标识、Cookie 文件名和第三方接口保持兼容。

### 性能与启动

- 低/中/高画质分别限制为 30/45/60 FPS，超高画质继续跟随显示器刷新率；高分辨率场景自动限制到 45 FPS。
- 窗口失焦时降到 24 FPS 并降低后台渲染分辨率，最小化或隐藏时保持 1 FPS，重新聚焦后自动恢复。
- 恢复 Chromium 后台节流，本地库扫描、玻璃纹理计算和 WebGL 预编译改为启动页退出后错峰执行。
- 修正高刷显示器的限帧节奏，减少安装完成后首次启动和后台运行时的 CPU 占用。

## v1.1.4

### 本地库、歌单与 Home

- 本地音乐按文件夹生成二级歌单，支持同目录封面/歌词、本地搜索、重启恢复和默认 MR 分析。
- 增加公开平台歌单导入、本地歌单、Home 紧凑歌单卡片和卡片排序。
- 自定义首页支持图片、文案、天气显示开关、定位和城市更新；天气不可用时整块隐藏。

### 桌面与播放体验

- 增加托盘控制、关闭到托盘、开机启动、沉浸自动全屏、倍速/音调和均衡器。
- 增加局内歌词行数/竖排、壁纸镜像和 Wallpaper Engine 素材浏览。
- 修复切换平台残留旧歌单详情、本地歌曲恢复播放和空天气数值显示异常。

## v1.1.3

### 智能过渡画面

- “镜头与叠加”里新增智能过渡画面选择：镜面、频谱、方块、右滑、随机、关闭。
- 默认使用镜面过渡；随机模式会在自然播放触发智能过渡时从现有效果里抽取一种。

## v1.1.2

### 评论区

- 红心按钮右侧新增评论按钮，显示评论数量，最多显示为 `999+`。
- 点击评论按钮会打开评论面板。
- 评论面板支持小云和小Q评论接口。

### 智能过渡 / Crossfade

- 歌曲切换新增智能过渡效果
- 同时重绑 WebAudio 频谱与音量链路，避免视觉响应或音量控制断掉。
- 底部“智能过渡”标识

### 继续听 / Home

- “继续听”优先接上上次恢复的播放队列和当前歌曲。
- 显示当前恢复队列里的歌曲、封面、歌手和队列位置。
- 点击“继续听”会从恢复队列当前位置继续播放，不再新建单曲队列。

### 其他修正

- 点击歌曲详情时保持二级面板逻辑。
- Home 卡片部分英文标签改为中文。

## v1.1.1

- P0 installer safety fix: installation now defaults to the first available non-C drive from `D:\Mineradio` through `Z:\Mineradio`; it falls back to `C:\Mineradio` only when no D-Z drive exists.
- The installer now forces the target path into a dedicated `Mineradio` folder, blocks non-empty non-Mineradio-owned targets, and blocks C drive installs when a D-Z drive is available.
- Existing registered installs in a dedicated `...\Mineradio` folder are adopted in place so users can run the new setup over their current installation and receive the safe uninstaller.
- Existing dedicated `...\Mineradio` folders that already contain Mineradio files can be overwritten even if they were created before the new safety marker existed.
- The new uninstaller no longer removes the whole installation root or app subfolders recursively; it deletes only known top-level Mineradio/Electron files and leaves unrelated files in place.
- Legacy uninstallers without the new safety marker are skipped during install; the setup deletes only the old `Uninstall Mineradio.exe` file and registry entry so it cannot indirectly trigger an older unsafe uninstall routine.
- Installer/uninstaller safety fixes require the full setup package; quick patch JSON files must not be used as the only delivery path for this issue.

## v1.1.0

- 纯净安装发布版：从当前 `resources/app` 可信源码重新构建 1.1.0 安装包，旧打包产物、旧备份包和旧安装包不再作为发布来源。
- 重要安全说明：`v1.0.10` 及更早旧安装包不再建议继续安装或传播，请先隔离旧安装包；本次不提供从 `v1.0.10` 到 `v1.1.0` 的软件内本地更新，请到 GitHub Release 手动下载 `v1.1.0` 进行纯净安装。
- 默认视觉参数更新：将 `默认测试.json` 作为首次启动默认用户存档和软件内视觉参数默认值；新用户首次启动即使用同一份快照，用户存档中自动预置「默认测试」。
- 默认视觉快照包括：`emily` 视觉预设、封面粒子分辨率 `1.55`、电影镜头强度 `0.5`、歌词发光 `0.28`、高亮色 `#fac900`、发光色 `#008aff`、UI/Home/视觉图标白色、3D 歌单架静态常驻、播客歌单默认关闭、侧向角度 `-15`。
- 3D 歌单架新增内容开关：可关闭播客歌单，可选择是否把「我的歌单」与「收藏歌单」合并成一条连续滚动列表。
- 3D 歌单架交互修复：动态/静态详情页绑定分流，静态详情页跟随封面粒子世界轴，动态详情页跟随镜头；常驻歌单架未命中时不再长期挡歌词，命中卡片时才浮起到前景。
- 3D 歌单详情页修复：详情页打开后保持更完整的页面显示和中心高亮行；修复固定/常驻状态下详情页被歌词或卡片遮挡、动态镜头触发区异常、滚轮高亮不同步等问题。
- 歌词透明度修复：详情页选歌/切歌时新词不再突然跳亮；详情页打开时歌词保持可读，但降低发光层并保持在详情页下层，避免遮挡中心高亮行。
- 高级设置新增性能项：后台策略支持自动优化、保持运行、停止释放；画质档位支持低/中/高/超高；直播后台保持开启后不再进入低占用暂停。
- 用户存档修复：应用用户视觉存档后会提交播放态视觉预设，跳转歌曲、切歌不再回退到上一个视觉预设。
- 全屏/控制台修复：视觉预设热键按钮不再被全屏 DIY 悬浮入口遮挡；视觉控制台隐藏按钮会跟随一起隐藏，不再残留一个孤立标记。
- 更新 Electron / electron-builder 依赖，修复已知高风险构建依赖告警。
- 修复软件内更新失败后的循环体验：快速补丁失败后不再自动反复拉完整安装包，完整安装包下载完成后也不再自动打开，需用户手动确认。
- 更新下载会复用本地已校验通过的安装包，避免重复下载同一文件。

## v1.0.10

- 重做桌面歌词视觉与交互：保持软件内歌词质感，优化白底可读性、长歌词滚动、锁定穿透、电影震动与最小化状态一致性。
- 修复安魂预设歌词位置无法调整的问题，当前嘴部歌词原位置作为原点，景深改为按视角前后移动。
- 歌词布局新增上下角度与左右角度调节，并会随本地布局和用户存档保存。
- 歌词镜头绑定开启时避让右侧 3D 歌单架，安魂预设也同步处理，避免遮挡歌单架操作。
- 新增方向键上下调节音量，每次 5%，输入框和滑条聚焦时不会误触。
- 调整更新包规则：补丁包命名使用 `旧版本→新版本` 表达，且以后只为低于新版的最近 4 个版本生成快速补丁。

## v1.0.9

- 修复安装包深色界面中部分文字仍为黑色导致看不清的问题，安装器改为浅色高对比界面。
- 安装包支持用户自由选择安装目录；默认仍优先 `D:\Mineradio`，选择盘根目录时会自动补全 `Mineradio` 文件夹。
- 修复软件可重复打开的问题，重复启动时会聚焦当前已运行的窗口。
- 修复软件每次启动都会自动创建/刷新桌面快捷方式的问题。

## v1.0.8

- 修复小Q登录后头像/昵称与播放授权状态不同步的问题，歌单能读但歌曲无法播放时会优先识别真实播放票据缺失。
- Home 左侧大卡片改为施工提示，并支持在 Home 页手动展开播放器控制台。
- 视觉预设顺序调整为 emily、安魂、星河、唱片、星球、滚筒、虚空；安魂十字架选中态保持同款蓝色。
- 视觉控制台新增 4 个槽位的“用户存档”，可保存粒子、颜色、滑条和歌词外观习惯，并支持卡片内直接命名。
- 修复重启后歌词预设/自定义颜色没有立即恢复到舞台歌词的问题。
- 播放与暂停增加音频淡入淡出，降低手动启停时的突兀感。

## v1.0.7

- 反正没什么人看，布想写日志了

## v1.0.6

- 反正没什么人看，布想写日志了

## v1.0.5

- 优化 GitHub 更新下载：默认优先尝试国内加速线路，失败后自动切换下一条线路，最后兜底 GitHub 直连。
- 更新弹窗新增下载速度、已下载大小、预计剩余时间、当前线路和线路尝试次数显示。
- 更新失败时保留具体失败原因和已尝试线路数量，方便用户判断是超时、DNS、HTTP、校验失败还是资源未同步。
- 下载完整安装包和快速补丁都会校验 release digest，避免镜像缓存异常导致坏包被应用。
- 修复旧版本快速补丁匹配过宽的问题，确保 `1.0.0`、`1.0.1`、`1.0.2`、`1.0.3` 只会匹配各自对应的补丁包。

## v1.0.4

- 优化最小化/隐藏窗口时的后台占用：只有真正最小化、不可见或文档隐藏时进入深度低占用；可见但失焦、副屏显示时保持正常帧率和电影镜头。
- 修复桌面全屏模式下 3D 视觉画布尺寸不同步导致的裁切/偏上问题；全屏、退出全屏和窗口 resize 后会连续刷新主渲染视口。
- 优化播放器控制台自动隐藏时的残影：隐藏状态不再保留玻璃背景、滤镜和阴影，同时保留显示时的原有玻璃质感。
- 视觉控制台新增“控制台玻璃色差”滑条，并给视觉控制台悬浮按钮增加贴边自动隐藏按钮。
- 左侧“我的歌单”详情页改为分批加载更多歌曲，放大“加载更多”按钮，详情头部支持滚动置顶和回到顶部。
- 沉浸模式恢复左侧歌单栏、3D 歌单架和默认封面渐变背景的可用性。

## v1.0.3

- 重构视觉预设控制台的信息结构，新增 `预设 / 外观 / 歌词 / 动态 / 高级` 五个分区，减少功能堆叠带来的操作压力。
- 优化色轮和封面取色面板的自适应定位，弹层会贴近触发按钮并限制在当前窗口内，降低不同分辨率下错位或越界的概率。
- 修复背景颜色状态混淆问题：纯黑色现在代表真正的黑色背景，封面渐变改为独立模式，控制台会明确显示“封面渐变”。
- 保留并优化根据专辑封面生成背景渐变的功能，避免点击默认时才触发、左侧色块却显示黑色的误导。

## v1.0.2

- Add collect-to-playlist and play-next actions to artist detail songs.
- Publish quick patches for `1.0.1 -> 1.0.2` and `1.0.0 -> 1.0.2`, plus a full installer for cross-version upgrades.

## v1.0.1

- Smooth Emily visual loading and track-change transitions.
- Remove the forced loading-state jump, shorten the Emily cover color mix, and fade cover depth in gradually.
- Publish a `1.0.0 -> 1.0.1` quick patch so installed users can receive an update prompt.

## v1.0.0

- 调整播放器控制台显隐规则：播放、切歌、天气电台开播不再主动弹出底部控制台；只有底部手柄/热区唤出时显示，鼠标移出热区后快速收起。
- 修复播放器控制台进度条不随播放推进的问题：进度条和时间显示现在会优先使用真实音频时长，并在浏览器暂未返回 duration 时使用歌曲 metadata 兜底。
- 修复小云 / 小Q扫码成功但登录态不落地的问题：小云不再因资料接口短暂无 profile 立刻清空 cookie，小Q登录支持新式 `psrf_*` / `wx*` token。
- 修复播放/暂停按钮手动恢复播放失效：手动点击路径现在会在用户点击同步栈内先调用 `audio.play()`，再恢复音频分析器，避免错过浏览器用户激活窗口。
- 修复播放准备阶段的栈溢出会拖死控制台的问题：歌词预置、封面加载、喜欢状态、Home 状态、listen session 和 beatmap / DJ 视觉预热均改为隔离执行，任何非音频链路异常不再阻断真实切歌。
- `playQueueAt` 增加播放阶段日志；若后续仍出现异常，控制台会标明 `source-url`、`visual-prep`、`audio-start` 等阶段，界面不再直接弹出 `Maximum call stack size exceeded`。
- 单曲循环的 `ended` 回调改为异步再入，避免浏览器在音频结束事件中同步重进 `playQueueAt` 引发递归风险。
- 修复天气电台 / 歌单播放链路的控制台递归风险：不再运行时重写播放、上一首、下一首按钮的 `onclick`，避免 `Maximum call stack size exceeded`。
- 切歌开始时立即刷新队列高亮和迷你队列计数；即使浏览器拦截自动播放，标题、时长、队列当前项也保持同步。
- Home 天气电台主按钮和天气卡片统一到同一启动入口，并增加启动 busy 锁，避免重复点击并发开播。
- 修复右键唤起 3D 歌单架时误唤出播放器控制台：歌单架打开期间会硬隐藏底部控制台，并阻止热区/播放态 reveal。

- 隔离 3D 歌单架刷新/关闭异常：歌单 API 成功后不再因为 shelf 重建或 3D 详情列表绘制失败而误报“歌单加载失败”。
- 天气电台、歌单、播客、每日推荐和 3D 详情播放入口统一使用安全队列刷新；队列写入后播放器控制台会恢复可点击，不再被 UI 渲染异常拖死。
- 恢复歌单 / 天气电台自动开播为非 manual 路径，并将 `playQueueAt` 前置 UI 阶段纳入兜底，避免队列标题已变但实际音频和上下一首按钮停在旧状态。
- 天气电台音乐来源收窄为小云搜索，并取消入队前批量播放 URL 探测，避免点击入口时请求堆积造成卡死。
- 天气电台前端复用正在进行的天气加载请求，避免 Home 后台预加载和手动启动同时打到后端。
- 自动播放被系统拦截时，播放器控制台保持可点击并提示手动继续；真正加载失败的队列项会自动跳到下一首。
- 手动下一首 / 上一首现在一定切换实际音频；单曲循环只在自然播完时重复当前曲。
- 切歌开始时立即暂停旧音频，避免出现歌词和 UI 已切走但旧歌还在播的错位。
- 修复天气电台城市经纬度缺失时落到 `(0,0)` 的问题，并在 Open-Meteo 超时/失败时返回临时电台队列。
- 歌单曲目加载增加 `playlist_track_all -> playlist_detail` 降级，减少歌单加载失败。
- 播放队列写入后强制恢复底部控制台可交互状态，避免天气电台或歌单开播后控制台残留不可点击。
- 启动页退出后补 Home 恢复安全网，避免首页隐藏在空场背景后。

- 发布 Mineradio 首个正式版本，GitHub 主页和安装包版本统一为 `1.0.0`。
- 首页正式版包含天气电台、每日推荐、私人电台、继续听、听歌画像和我的歌单入口。
- 天气电台接入 Open-Meteo，并优先混入每日推荐和私人推荐曲库，过滤 AI / 白噪音 / 雨声助眠等低质结果。
- 修复窗口模式下主页 6 张主卡片在紧凑尺寸中过早堆叠、挤出首屏的问题。
- 保持启动后 Wallpaper 银河背景干净显示，播放后切换到 Emily / 默认播放态视觉。

## v0.9.13

- 开场动画升级为 WebGL 光流线场，融合 `ShipSwiftAnimatedLoop` 的 RGB 分通道、Warp 流动和高亮线场质感。
- 移除刻意的环形/花瓣式爆点，改为更自然的斜向流线相位变化，让启动过程更帅气但不突兀。
- 开场动画播放完成后不再自动进入主页，会停留在“点击进入”状态；点击任意位置或按 Enter/空格后进入 Home。
- 保留 2D canvas fallback，WebGL 不可用时仍能显示启动页。
- 生成 `0.9.12 -> 0.9.13` 快速补丁，用于已安装用户轻量更新。

## v0.9.9

- 统一当前版本号为 `0.9.9`
- 接入 GitHub Releases 更新检测配置
- 增加更新弹窗与更新下载任务接口
- 增加小Q与小云双平台体验方向
- 优化电影镜头节奏分析与 DJ 视觉模式
- 增加自定义封面、自定义歌词、歌词布局和视觉控制相关能力

## 说明

这是首次公开发布前的整理版本。更早版本属于本地迭代记录，暂不展开。
